import { MessageSquare, Send, X } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { chatMessages } from "@/lib/game-mock";

export function GameChat() { const [open,setOpen] = useState(false); const [draft,setDraft] = useState(""); return <div className="absolute bottom-5 left-5 z-30">{open && <section className="mb-3 w-72 rounded-md border border-panel-border bg-panel-strong/95 p-3 shadow-2xl backdrop-blur-lg"><header className="flex items-center justify-between border-b border-panel-border pb-2"><h2 className="font-brand text-sm font-bold text-gold">SQUAD CHAT</h2><Button variant="ghost" size="icon" onClick={() => setOpen(false)} aria-label="Close chat" className="size-7"><X/></Button></header><div className="space-y-3 py-3">{chatMessages.map((message) => <p key={`${message.author}-${message.text}`} className="text-xs"><b className={message.colour}>{message.author}</b><br/><span className="text-muted-foreground">{message.text}</span></p>)}</div><form className="flex gap-2" onSubmit={(e) => { e.preventDefault(); setDraft(""); }}><Input value={draft} onChange={(e) => setDraft(e.target.value)} placeholder="Message squad..." className="h-9 bg-background/70"/><Button variant="game" size="icon" aria-label="Send message"><Send/></Button></form></section>}<Button variant="gameOutline" size="icon" className="relative size-11" onClick={() => setOpen(!open)} aria-label="Open chat"><MessageSquare/><span className="absolute -right-1 -top-1 flex size-4 items-center justify-center rounded-full bg-danger text-[9px] font-bold text-danger-foreground">2</span></Button></div>; }